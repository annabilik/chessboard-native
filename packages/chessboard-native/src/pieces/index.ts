/**
 * Published default artwork for the twelve standard chess piece keys.
 *
 * Renderer types remain available from the package root.
 *
 * @packageDocumentation
 */
export { defaultPieceRenderers } from './default-piece-renderers';

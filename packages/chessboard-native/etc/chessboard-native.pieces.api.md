## API Report File for "@vibechess/chessboard-native"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

import type { JSXElementConstructor } from 'react';
import type { ViewStyle } from 'react-native';

// Warning: (ae-forgotten-export) The symbol "PieceRenderers" needs to be exported by the entry point index.d.ts
//
// @public
export const defaultPieceRenderers: PieceRenderers;

// @public
type MoveSource = {
    readonly kind: 'board';
    readonly square: SquareId;
} | {
    readonly kind: 'spare';
    readonly spareId: string;
};

// @public
interface PieceData {
    readonly id?: string;
    // Warning: (ae-forgotten-export) The symbol "PieceType" needs to be exported by the entry point index.d.ts
    readonly pieceType: PieceType;
}

// Warning: (ae-forgotten-export) The symbol "PieceRendererProps" needs to be exported by the entry point index.d.ts
//
// @public
type PieceRenderer = JSXElementConstructor<PieceRendererProps>;

// @public
type PieceRendererProps = {
    readonly boardId: string;
    readonly piece: PieceData;
    readonly size: number;
    readonly state: PieceVisualState;
    readonly style: Readonly<ViewStyle>;
} & ({
    readonly source: Extract<MoveSource, {
        readonly kind: 'board';
    }>;
    readonly square: SquareId;
} | {
    readonly source: Extract<MoveSource, {
        readonly kind: 'spare';
    }>;
    readonly square: SquareId | null;
});

// Warning: (ae-forgotten-export) The symbol "PieceRenderer" needs to be exported by the entry point index.d.ts
//
// @public
type PieceRenderers = Readonly<Partial<Record<PieceType, PieceRenderer>>>;

// @public
type PieceType = string;

// @public
interface PieceVisualState {
    readonly isDragging: boolean;
    readonly isGhost: boolean;
    readonly isPending: boolean;
    readonly isPressed: boolean;
    readonly isTransitioning: boolean;
}

// @public
type SquareId = string;

// Warnings were encountered during analysis:
//
// src/public-types.ts:150:31 - (ae-forgotten-export) The symbol "SquareId" needs to be exported by the entry point index.d.ts
// src/public-types.ts:601:3 - (ae-forgotten-export) The symbol "PieceData" needs to be exported by the entry point index.d.ts
// src/public-types.ts:603:3 - (ae-forgotten-export) The symbol "PieceVisualState" needs to be exported by the entry point index.d.ts
// src/public-types.ts:608:7 - (ae-forgotten-export) The symbol "MoveSource" needs to be exported by the entry point index.d.ts

```

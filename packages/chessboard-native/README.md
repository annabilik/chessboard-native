# @vibechess/chessboard-native

A controlled, rules-free React Native chessboard component.

This package is an unpublished Phase 0 shell. It currently exposes only a
disabled visual frame used to verify package builds and Expo workspace
consumption. Position data, annotations, interaction, and the stable public API
will land in later phases.

Do not use this package in production yet.
